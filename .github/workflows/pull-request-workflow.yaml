name: build docker images
on:
  pull_request: {}

permissions:
  contents: read
  packages: write

jobs: 
  build-images:
    uses: ./.github/workflows/build-images-workflow.yaml
    with:
      push: false
      ref: ${{ github.ref }}